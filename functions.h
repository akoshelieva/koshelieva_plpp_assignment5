#ifndef FUNCTIONS_H
#define FUNCTIONS_H
#include <cmath>
namespace mathlib {
    double Pow (double base, double exp);
    double Abs (double x);
    double Max (double a, double b);
    double Min (double a, double b);
}

#endif //FUNCTIONS_H
